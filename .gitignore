# Internal compliance notes and strategy docs — never publish to the public repo or website
COMPLIANCE_NOTES.md
SEO-BASELINE-*.md

# Agent-local configuration — never commit
.claude/

# Wrangler local state, secrets, and deps — never commit
.dev.vars
.wrangler/
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
