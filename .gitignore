# Internal compliance notes — never publish to the public repo or website
COMPLIANCE_NOTES.md

# Agent-local configuration — never commit
.claude/

# Wrangler local state and deps — never commit
.wrangler/
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db

.dev.vars*
!.dev.vars.example
.env*
!.env.example
